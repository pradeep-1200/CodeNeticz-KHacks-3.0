# Python Dependencies for Dyslexia AI Assistants

# Core ML/NLP
transformers>=4.30.0
torch>=2.0.0
requests>=2.28.0

# Text Processing
nltk>=3.8.0
spacy>=3.5.0

# Optional: For faster inference
accelerate>=0.20.0

# Note: Install these in the Dyslexia/venv virtual environment
# 
# To recreate the virtual environment:
# 1. cd Dyslexia
# 2. python -m venv venv
# 3. venv\Scripts\activate (Windows) or source venv/bin/activate (Linux/Mac)
# 4. pip install -r requirements.txt
